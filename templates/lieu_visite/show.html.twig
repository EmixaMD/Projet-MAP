{% extends 'base.html.twig' %}

{% block title %}LieuVisite{% endblock %}

{% block body %}
    <h1>LieuVisite</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ lieu_visite.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ lieu_visite.nom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('lieu_visite_index') }}">back to list</a>

    <a href="{{ path('lieu_visite_edit', {'id': lieu_visite.id}) }}">edit</a>

    {{ include('lieu_visite/_delete_form.html.twig') }}
{% endblock %}
