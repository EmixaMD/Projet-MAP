{% extends 'base.html.twig' %}

{% block title %}Visite index{% endblock %}

{% block body %}
    <h1>Visite index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>heureArrivee</th>
                <th>heureDepart</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for visite in visites %}
            <tr>
                <td>{{ visite.id }}</td>
                <td>{{ visite.heureArrivee ? visite.heureArrivee|date('Y-m-d H:i:s') : '' }}</td>
                <td>{{ visite.heureDepart ? visite.heureDepart|date('Y-m-d H:i:s') : '' }}</td>
                <td>
                    <a href="{{ path('visite_show', {'id': visite.id}) }}">show</a>
                    <a href="{{ path('visite_edit', {'id': visite.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('visite_new') }}">Create new</a>
{% endblock %}
