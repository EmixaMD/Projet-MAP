{% extends 'base.html.twig' %}

{% block title %}LieuVisite{% endblock %}

{% block body %}
    <h1>LieuVisite</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ admin_lieu_visite.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ admin_lieu_visite.nom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_lieu_visite_index') }}">back to list</a>

    <a href="{{ path('admin_lieu_visite_edit', {'id': admin_lieu_visite.id}) }}">edit</a>

    {{ include('admin_lieu_visite/_delete_form.html.twig') }}
{% endblock %}
