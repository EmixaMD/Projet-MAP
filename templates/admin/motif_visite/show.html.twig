{% extends 'base.html.twig' %}

{% block title %}MotifVisite{% endblock %}

{% block body %}
    <h1>MotifVisite</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ motif_visite.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ motif_visite.nom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('motif_visite_index') }}">back to list</a>

    <a href="{{ path('motif_visite_edit', {'id': motif_visite.id}) }}">edit</a>

    {{ include('motif_visite/_delete_form.html.twig') }}
{% endblock %}
